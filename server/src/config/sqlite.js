/**
 * ARCHIVO LEGACY — Ya no se utiliza.
 * El proyecto ahora usa PostgreSQL con Sequelize.
 * Ver: src/models/index.js para la conexión actual.
 * 
 * Se mantiene como referencia de la configuración anterior con SQLite.
 */

// Este módulo fue reemplazado por Sequelize + PostgreSQL
// La conexión actual se encuentra en src/models/index.js
module.exports = null;
